# Candidate Feedback Groups

This folder stores KEL v0.2 de-duplicated groups produced by
`tools/kel/group_feedback.py`.

Every group preserves all atomic and source feedback IDs. A topic-key match that
is not an exact fingerprint match remains flagged for review.
